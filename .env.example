# PostgreSQL 数据库配置
# 复制此文件为 .env 并填入实际值：cp .env.example .env

DB_HOST=localhost
DB_PORT=5432
DB_NAME=ginkgobrain
DB_USER=postgres
DB_PASSWORD=

# 连接池配置（可选，保持默认即可）
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30
